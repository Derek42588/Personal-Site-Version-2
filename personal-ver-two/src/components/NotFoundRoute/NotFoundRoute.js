import React from 'react';
import classes from './NotFoundRoute.module.css'

const NotFoundRoute = (props) => (
    <div classeName = {classes.NotFound}>
        <h3>Sorry, I can't find that page !</h3>
    </div>
    );

export default NotFoundRoute;